# src/app

## 作用
Next.js App Router 的根目录，负责全局布局、样式和顶层路由组织。

## 职责
- 定义应用级布局与通用样式。
- 承载顶层页面与子路由入口。
- 包含 API 路由与 UI 路由的目录结构。

## 内容
- 子目录：api、app
- 文件：favicon.ico、globals.css、layout.tsx、not-found.tsx、page.tsx
